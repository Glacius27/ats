{{- if .Values.metrics.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "ats-vacancy-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ include "ats-vacancy-service.fullname" . }}
    release: {{ .Release.Name }}
spec:
  selector:
    matchLabels:
      app: {{ include "ats-vacancy-service.fullname" . }}
  endpoints:
    - port: http
      path: /metrics
      interval: {{ .Values.metrics.scrapeInterval | default "30s" }}
{{- end }}

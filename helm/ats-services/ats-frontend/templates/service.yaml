apiVersion: v1
kind: Service
metadata:
  name: {{ include "ats-frontend.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
      name: http
  selector:
    app: {{ include "ats-frontend.fullname" . }}

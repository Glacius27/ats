apiVersion: v1
kind: Secret
metadata:
  name: {{ include "authorization-service.fullname" . }}-secret
type: Opaque
stringData:
{{- range $key, $value := .Values.secretEnv }}
  {{ $key }}: "{{ $value }}"
{{- end }}
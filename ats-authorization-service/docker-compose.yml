version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: ats-authorization-db
    environment:
      POSTGRES_USER: ats
      POSTGRES_PASSWORD: ats_pass
      POSTGRES_DB: ats_auth
    ports:
      - "5434:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
